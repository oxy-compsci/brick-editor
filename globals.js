// global variables

// the Monaco editor instance
var editor = null; // eslint-disable-line no-unused-vars

// whether the editor buffer is parsable
var parsable = true; // eslint-disable-line no-unused-vars
// the editor buffer at the last parsable point
var parsableText = ""; // eslint-disable-line no-unused-vars
// the position of the cursor/selection at the last parsable point
var cursor = null; // eslint-disable-line no-unused-vars
